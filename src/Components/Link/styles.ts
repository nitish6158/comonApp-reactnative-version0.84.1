import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  icon: {
    marginRight: 12,
  },
  link: {
    alignItems: "center",
    flexDirection: "row",
    marginBottom: 12,
    paddingVertical: 6,
  },
});

export default styles;
